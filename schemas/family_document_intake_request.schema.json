{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://skeleton.local/schemas/family_document_intake_request.schema.json",
  "title": "Private family document intake request",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "operation"],
  "properties": {
    "schema": {"const": "skeleton.family_document_intake.request.v1"},
    "operation": {"enum": ["plan", "process", "reconcile", "status"]},
    "source": {"type": "string", "maxLength": 4096},
    "packet_path": {"type": "string", "maxLength": 4096}
  }
}
