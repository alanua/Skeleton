# Lavalamp Source Placeholder

This directory intentionally contains no implementation yet.

Future source changes must:

- read the relevant WLED effect or usermod code before editing;
- keep cylinder coordinate-system assumptions explicit;
- include source files only, not generated output;
- avoid device-specific configuration, secrets, Wi-Fi details, OTA material, and private logs.

