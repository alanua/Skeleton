# Lavalamp Firmware Placeholder

This directory intentionally contains no firmware artifacts.

Future firmware work may add reviewed source-level build instructions or configuration templates with placeholder values only. It must not add compiled firmware, OTA payloads, PlatformIO build cache, credentials, device-specific configuration, or private runtime data.
