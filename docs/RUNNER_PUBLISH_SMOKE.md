# Runner Publish Smoke

This file records a tiny docs-only smoke change for the Runner publish path.

Scope:

- Classification: `PUBLIC_SAFE_DOCS_ONLY`
- Changed file: `docs/RUNNER_PUBLISH_SMOKE.md`
- Runtime impact: none
- Secret handling: no secrets added or referenced

Purpose:

Verify that Runner can push a branch and open a draft pull request after stale `GITHUB_TOKEN` removal.
