# Review status

The manual recovery branch is intentionally not merge-ready until its invocation arguments are checked against the exact current `scripts/home_edge_exec.py` CLI contract and CI passes. Do not deploy this bridge from this branch before that validation.
