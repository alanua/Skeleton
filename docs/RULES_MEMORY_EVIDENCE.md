# Memory Activation Evidence

Classification: CANON_BEHAVIOR, ADAPTIVE_LESSON, and WORKFLOW_RULE.
Rank: P1.
Project: skeleton.

memory_manager exists and is tested.
memory_store exists and is tested.
memory_manager_live_storage is planned and untested.

Correct rule: memory does exist as stage 1 routing and local ledger support. The missing part is automatic workflow activation.

Every fixuy request is a memory routing event: classify first, then route as weak cache, project state, canon candidate, confirmed canon, private sensitive, or rejected outdated.

Public-safe durable changes go to GitHub review. Private-sensitive content stays out of public GitHub. Canon and instruction changes still require critique and operator approval.

Direct adaptive execution rule:

When Oleksii says fixuy / фіксуй / зафіксуй together with an instruction to adapt, the next assistant step must change the working route immediately. Do not only write a note. Do not create duplicate planning tasks. Apply the lesson to the next action.

Current applied behavior:

- repeated blockers become ACTIVE_BLOCKER;
- broken Runner publishing is not used for new content tasks;
- small public-safe fixes use GitHub connector when available;
- project strategy and working strategy are kept separate;
- memory_manager and memory_store are treated as available stage 1 memory tools;
- every future fixuy response must report only classification, route, and next practical action.

Next practical need: connect fixuy requests to memory_manager and memory_store with an explicit local ledger path, tests, and no automatic canon promotion.
