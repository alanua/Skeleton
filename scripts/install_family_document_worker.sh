#!/usr/bin/env bash
set -euo pipefail

cat <<'MSG'
This repository ships the family document worker unit file only.
Install it manually after reviewing local paths and private runtime policy:
  ops/systemd/skeleton-family-document-intake.service
MSG
