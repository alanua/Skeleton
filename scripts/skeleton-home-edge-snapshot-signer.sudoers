# Managed by skeleton Home Edge snapshot signer installer.
# The installer replaces __RUNNER_USER__ with the configured Runner service account.
__RUNNER_USER__ ALL=(root) NOPASSWD: /usr/local/sbin/home_edge_media_source_snapshot_signer
